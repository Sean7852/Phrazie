"Please update the UI for each state block (Normal, Break, Drop) to match the 'StateInstance.jpg' design exactly. Follow these requirements:

State Color Indicator: Add a large, solid-colored rounded square directly beneath each state name on the far left.

Maximized Thumbnails: Display video clips as a horizontal row of rounded-corner squares. These thumbnails must fill the entire height of the state block, leaving only a small margin at the top and bottom.

Action Button Group: Move the 'Edit' and 'Remove' buttons to the far right of each state row, stacked vertically.

Minimalist Styling: The 'Remove' button text should be a dimmed red, and the 'Edit' button should be a neutral dark gray to maintain the 'Subtle futuristic tone'.


Functionality: Clicking 'Edit' or the color square must open a modal for renaming and color picking."