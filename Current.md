I need to implement the dual-screen club output feature in Phrazie. This involves creating a dedicated output window and adding a control in the UI to toggle it.1. The Projector Toggle:
* In PlaybackControlView.axaml, add a new button next to the REC control or in the BPM group.Visuals: Use a monitor icon or the text 'PROJECT'.
* Styles: When active, give it a subtle amber glow ({StaticResource AccentBrush}) to match the industrial design.2. The External Window (Club Output):
* Create ExternalOutputView.axaml as a borderless, full-screen window.
* It should render the Persistent Video Buffer directly using the high-performance rendering strategy we discussed (avoiding NativeControlHost limitations).
* Ensure it is completely decoupled from the main UI thread to prevent stutters when I navigate the Clip Queue or Collections.3. Window Management Logic:Implement a ProjectionService that detects all connected displays.When 'PROJECT' is toggled ON:Identify the second monitor (or primary if only one is connected).Launch the ExternalOutputView and snap it to full-screen on that monitor.When toggled OFF: Close the window gracefully.4. Animation & Polish:Add a 0.5s Cross-fade transition on the external window when the clip changes, so the club screen doesn't snap harshly between visuals.