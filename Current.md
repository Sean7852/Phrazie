"Add State" Button: As previously proposed, the "Add State" button should be replaced centered, half of the normal button width, keep the current height

State UI Refinement:
1. Check the StateInstance.png, this is what each state panel looks like. Click Edit will shows up a windows allows the users to change the name and pick a new color in the color picker.
2. Remove the Assign Clip & Loop buttons.
3. Horizontal Clip Scrollable List (Content Area):The main area of each state block should be dedicated to the horizontal clip scrollable list For the initial MVP simulation, use default rounded-corner square images as placeholders for video clips.These images must be arranged in a horizontal row, allowing users to easily scroll left and right for preview purposes.Ensure all buttons are clear, simple, and performance-focused, as defined in the brand visual guidelines.
